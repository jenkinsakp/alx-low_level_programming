#include <stdio.h>
/**
* main - write a program that prints all single digit numbers of base 10 starting from 0, followed by a new line.
*
* Description: All your code should be in the main function
* this program prints "write a program that prints all single digit numbers of base 10 starting from 0, followed by a new line.
* Return: 0
*/
int main(void)
{
int num;
for (num = 0; num < 10; num++)
{
	printf("%num", num);
}
printf("\n");
return (0);
}
