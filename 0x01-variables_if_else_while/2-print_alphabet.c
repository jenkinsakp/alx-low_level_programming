#include <stdio.h>
/**
* print number if is + (postive),0 ( zero), or - (negative)
*
*Description: us the main function
*
*/
int main(void)
{
char akp;

for (akp = 'a' ; ch <= 'z' ; akp++)
{
	putchar(akp);
}
putchar('\n');
return (0);
}
