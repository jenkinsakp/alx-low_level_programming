#include <stdio.h>
/**
* print number if is + (postive),0 ( zero), or - (negative)
*
*  us the main function
*
*/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
